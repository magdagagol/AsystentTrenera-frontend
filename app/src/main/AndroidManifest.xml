<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.asystenttrenera_frontend" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AsystentTrenerafrontend"
        android:usesCleartextTraffic="true" >
        <activity
            android:name=".group.AddParticipants"
            android:exported="false" />
        <activity
            android:name=".group.GroupDetails"
            android:exported="false" />
        <activity
            android:name=".group.GroupsActivity"
            android:exported="false" />
        <activity
            android:name=".participant.ParticipantDetails"
            android:exported="false" />
        <activity android:name=".zawodnik.EdytujZawodnika" />
        <activity android:name=".zawodnik.DodajZawodnika" />
        <activity android:name=".zawodnik.ZawodnikDetails" />
        <activity android:name=".zawodnik.ZawodnikActivity" />
        <activity android:name=".participant.ParticipantActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>